<a-waves aria-hidden="true" transition:persist="waves" transition:name="waves"><canvas></canvas></a-waves>

<style>
  a-waves {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
    contain: strict;
    opacity: var(--waves-opacity, .08);
    transition: opacity .6s ease;
  }
  a-waves canvas {
    display: block;
    width: 100%;
    height: 100%;
    color: rgb(var(--goo-color, 148, 234, 212));
  }
  @media (prefers-reduced-motion: reduce) {
    a-waves { display: none; }
  }
</style>

<script>
  import '../scripts/waves.js';
</script>
